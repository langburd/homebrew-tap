# langburd/homebrew-tap

Homebrew tap for [`set-tf-alias`](https://github.com/langburd/set-tf-alias).

## Install

```bash
brew tap langburd/tap
brew install set-tf-alias
```

Formulae are bumped automatically by the upstream `set-tf-alias` release
workflow via PRs against this repo.
